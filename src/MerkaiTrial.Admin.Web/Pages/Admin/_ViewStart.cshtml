@* =====================================================================
   FILE: MerkaiTrial.Admin.Web/Pages/Admin/_ViewStart.cshtml  (unchanged)

   Also: DELETE Pages/Admin/_ViewStart.cshtml.cs. That _ViewStartModel
   class came from the "Add Razor Page" scaffolder. _ViewStart.cshtml has
   no @@page directive and no model, so the class is never instantiated —
   dead code that implies wiring which does not exist.
   ===================================================================== *@
@{
    Layout = "_SuperAdminLayout";
}
