@* =====================================================================
   FILE: MerkaiTrial.Admin.Web/Pages/Account/Denied.cshtml

   COMPLETE FILE — replaces the existing one.

   Nothing renders here any more. OnGet redirects to /AccessDenied, which
   is now the single access-denied page. See Denied.cshtml.cs for why this
   file still exists at all rather than being deleted.

   No Layout is set on purpose: the redirect happens before any layout
   would be chosen, so naming one would only be a line to keep correct.
   ===================================================================== *@
@page
@model MerkaiTrial.Admin.Web.Pages.Account.DeniedModel
